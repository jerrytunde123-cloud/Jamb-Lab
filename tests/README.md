# Tests

```bash
npm install
npm test
```

- `utils.test.js` — dates, shuffle, toast, DOM helpers, `formatTime`
- `state.test.js` — config and state setters
- `points.test.js` — earn / spend / start button
- `questions.test.js` — equal subject split, bank names, image paths
- `smoke.test.js` — home → unlock → quiz → results against real markup
